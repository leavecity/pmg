# Windsurf Integration

Recommended workflow:

1. Keep PMG files in repository context.
2. Use `pmg context build --task "<task>"` for focused work.
3. Prefer ADRs and confirmed memory when resolving conflicting guidance.
4. Add review artifacts for security, dependency, performance, and architecture-sensitive work.

Suggested instruction:

```text
Use PMG as durable project memory. Load only the task-specific context bundle unless the user asks for broader project discovery.
```
