# Skill: Performance Reviewer

Use when work touches hot paths, rendering loops, large data, network calls, queries, or bundle size.

Responsibilities:

- identify expensive loops
- identify unnecessary re-renders
- identify N+1 queries
- identify memory leaks
- record durable performance constraints
